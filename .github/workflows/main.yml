name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    name: 🧭 Deploy to Dev
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: 📦 Print environment
        run: echo "Deploying to ${{ github.environment }}"

      - name: 🔑 Use Secret API Key
        env:
          MY_API_KEY: ${{ secrets.MY_API_KEY }}
        run: |
          echo "Using API key (masked): $MY_API_KEY"
